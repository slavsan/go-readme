readme:
  items:
    - name: title
      enabled: true
    - name: description
      enabled: true
    - name: license
      enabled: false
    - name: author
      enabled: true
    - name: setup
      enabled: true
  title: go-readme
  description: A cli utility for creating project READMEs
  badges:
  screenshots:
  project_status:
  prerequisites:
  getting_started:
  install:
  usage: |
    In order to generate a README.md file from a readme.yaml, run
    ```
    go-readme create
    ```
  setup:
    clone: |
      ```
      git clone git@github.com:slavsan/go-readme.git
      ```
    deps:
    dev:
    build: |
      ```
      go build main.go
      ```
    start: |
      ```
      go run main.go
      ```
    testing: |
      ```
      godog
      ```
    linting: |
      ```
      golangci-lint run
      ```
    coverage:
    static_analysis:
    swagger:
    deploy:
    release:
  examples:
  versioning:
  changelog:
  contributing:
  contributors:
  changelog:
  releases:
  contact:
  author: Slavcho Slavchev
  license: MIT
  acknowledgements:
